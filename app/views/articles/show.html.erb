<h1><%= @article.title %></h1>
<p><%= @article.body %></p>
<p>
  Tags: 
  <% @article.tag_list.each do |tag| %>
    <%= link_to tag, tag_path(tag), class: 'btn' %>
  <% end %>
</p>
<%= link_to  "Back to main", root_path %>